{
  "name": "@redread/extension",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "lint:firefox": "web-ext lint --source-dir dist/firefox"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.0",
    "esbuild": "^0.28.0",
    "sharp": "^0.34.0",
    "web-ext": "^10.6.0"
  }
}
